@startuml backend_minio
title Backend ↔ MinIO Communication

skinparam sequence {
    ParticipantBackgroundColor #e3f2fd
    ParticipantBorderColor #1565c0
}

participant "Backend API" as B
database "MinIO (S3)" as M

group Upload Artifact
    B -> M : PUT object\n(bucket, key, data)
    M --> B : 200 OK
end

group Download Artifact
    B -> M : GET object\n(bucket, key)
    M --> B : Binary stream
end

group Check Metadata
    B -> M : HEAD object\n(bucket, key)
    M --> B : Size, ContentType, ETag
end

note right of M
  **Protocol:** S3 API over HTTP
  **Auth:** Access Key + Secret Key
  **Buckets:** argo-artifacts
end note

@enduml
