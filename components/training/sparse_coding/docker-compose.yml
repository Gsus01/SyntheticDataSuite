services:
  entrenar:
    build: .
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data/battery_RUL_with_ID_pocos_datos.csv:/data/inputs/battery_RUL_with_ID_pocos_datos.csv:ro
      - ./model:/data/outputs
      - ./variables.json:/data/config/variables.json:ro
